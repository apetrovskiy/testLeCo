package challenges.c20210101_20210107.CheckArrayFormationThroughConcatenation

class SolutionKt {
    fun canFormArray(arr: IntArray, pieces: Array<IntArray>): Boolean {
        // for ktlint only
        listOf(arr).forEach { println(it) }
        listOf(pieces).forEach { println(it) }

        return true
    }
}
